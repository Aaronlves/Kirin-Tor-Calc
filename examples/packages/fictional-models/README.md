# Fictional Models Package

This data-only acceptance Package provides `fictional.theorycraft-model` revision 1. It exists to
exercise Package interface identity, Plugin compatibility checks, and revision-bound Catalog
queries. Its `build.kirin` data-only template declares one structured `coefficient` binding for
Plugin Proposal 2 acceptance. It contains no real game facts and executes no code.
